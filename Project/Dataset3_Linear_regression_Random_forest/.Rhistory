x <- rnorm (50)
y <- rnorm(x)
plot(y,x)
rm(y)
x
x<- NA
x
typeof(x)
x<-NULL
typeof(x)
?rnorm
x >3 & x > 7
x >3 & x < 7
x
x <- c(1,3,2,6,36,34)
x > 3 & x < 7
x[x > 3 & x < 7]
x[x > mean(x)]
clear
sd(x)
s <- sd
s <- sd(x)
x[x > (mean(x) + s) & x < (mean(x)-s)]
paste(c("x","y",1:10,sep-""))
paste(c("x","y",1:10,sep=""))
paste(c("x","y",1:10,sep=" "))
paste(c("x","y"),1:10,sep=" ")
paste(c("x","y"),1:10,sep=",")
x <- NA
x == NA
is.na(x)
library(mice)
mice_mod <-mice(titanicData[, !names(titanicData) %in%c('PassengerId','Name','Ticket','Cabin','Survived')], method='rf')
install.packages(c("ggplot2", "ggthemes", "scales", "dplyr", "mice", "randomForest"))
install.packages(c("ggplot2", "ggthemes", "scales", "dplyr", "mice", "randomForest"))
library(caTools)
library(randomForest)
genre_df <- genre_data[-c(1)]
genre_data <- na.omit(genre_data)
str(genre_df)
sample = sample.split(genre_df,SplitRatio = 0.01)
#train and Test
train <- subset(genre_df,sample == TRUE)
setwd("/home/rajesh/Desktop/DMML_project/Datasets/3/")
library(Amelia)
genre_data <- read.table(file='songDb.tsv', sep = '\t', header = TRUE, fill = TRUE)
str(genre_data)
missmap(genre_data,main = "missing in genre dataset")
sapply(genre_data, function(x) sum(is.na(x)))
genre_data <- na.omit(genre_data)
genre_data <- genre_data[-c(13:17)]
boxplot(genre_data$Loudness)
write.csv(genre_data,"/home/rajesh/genre_cleaned_data.csv")
head(genre_data,20)
typeof(genre_data$time_signature)
max(genre_data$Liveness)
genre_data <- genre_data[-c(13)]
genre_data$Genre <- as.factor(genre_data$Genre)
genre_data$time_signature <- as.factor(genre_data$time_signature)
genre_data$Tempo <- as.numeric(genre_data$Tempo)
str(genre_data)
unique(genre_data$Key)
summary(genre_data)
length(levels(genre_data$Genre))
library(caTools)
library(randomForest)
genre_df <- genre_data[-c(1)]
genre_data <- na.omit(genre_data)
str(genre_df)
sample = sample.split(genre_df,SplitRatio = 0.01)
#train and Test
train <- subset(genre_df,sample == TRUE)
test <- subset(genre_df,sample==FALSE)
#modeling the the training data
train$Genre <- factor(train$Genre)
randfmodel <- randomForest(Genre ~.,data = train, ntree = 2001,importance = TRUE)
library(randomForest)
genre_df <- genre_data[-c(1)]
genre_data <- na.omit(genre_data)
str(genre_df)
sample = sample.split(genre_df,SplitRatio = 0.1)
#train and Test
train <- subset(genre_df,sample == TRUE)
test <- subset(genre_df,sample==FALSE)
#modeling the the training data
train$Genre <- factor(train$Genre)
randfmodel <- randomForest(Genre ~.,data = train, ntree = 2001,importance = TRUE)
genre_df <- na.omit(genre_df)
boxplot(genre_data)
genre_df <- as.numeric(genre_data$time_signature)
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
genre_df$time_signature <- as.numeric(genre_data$time_signature)
genre_df$Genre <- as.numeric(genre_df$Genre)
str(genre_df)
boxplot(genre_df)
boxplot(genre_df)
boxplot(genre_df,outline = FALSE)
boxplot(genre_df)
boxplot(genre_df)
outliers <- boxplot(genre_df, plot=FALSE)$out
outliers
outliers <- boxplot(genre_df$Danceability, plot=FALSE)$out
outliers
#removing outliers
outliers <- boxplot(genre_df$Danceability, plot=FALSE)$out
x<-genre_df
#removing outliers
outliers <- boxplot(genre_df$Danceability, plot=FALSE)$out
x<- x[-which(x$Danceability %in% outliers),]
#removing outliers
outliers <- boxplot(genre_df$Energy, plot=FALSE)$out
x<-genre_df
x<- x[-which(x$Danceability %in% outliers),]
x<- x[-which(x$Energy %in% outliers),]
boxplot(genre_df)
#removing outliers
outliers <- boxplot(genre_df$Danceability, plot=FALSE)$out
x<-genre_df
x<- x[-which(x$Danceability %in% outliers),]
boxplot(x)
#removing outliers
outliers <- boxplot(genre_df$Key, plot=FALSE)$out
x<- x[-which(x$Key %in% outliers),]
boxplot(x)
#removing outliers
outliers <- boxplot(genre_df$Loudness, plot=FALSE)$out
x<- x[-which(x$Loudness %in% outliers),]
boxplot(x)
x<-genre_df
x<- x[-which(x$Loudness %in% outliers),]
boxplot(x)
#removing outliers
outliers <- boxplot(genre_df$Loudness, plot=FALSE)$out
x<-genre_df
x<- x[-which(x$Loudness %in% outliers),]
boxplot(x)
#removing outliers
outliers <- boxplot(genre_df$Danceability, plot=FALSE)$out
x<- x[-which(x$Danceability %in% outliers),]
boxplot(x)
#removing outliers
outliers <- boxplot(genre_df$Danceability, plot=FALSE)$out
x<-genre_df
x<- x[-which(x$Danceability %in% outliers),]
boxplot(x)
#removing outliers
outliers <- boxplot(genre_df$Key, plot=FALSE)$out
x<-genre_df
x<- x[-which(x$Key %in% outliers),]
boxplot(x)
x<-genre_df
boxplot(x)
library(outliers)
outliers <- boxplot(genre_df$Key, plot=FALSE)$out
x<-genre_df
x<- x[-which(x$Key %in% outliers),]
#removing outliers
rm.outlier(genre_df,fill = FALSE, median = FALSE, opposite = FALSE)
#removing outliers
rm.outlier(genre_df)
#removing outliers
rm.outlier(genre_df$Danceability)
#removing outliers
genre_data$Danceability <- rm.outlier(genre_df$Danceability)
#removing outliers
genre_df$Danceability <- rm.outlier(genre_df$Danceability)
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
genre_df$time_signature <- as.numeric(genre_data$time_signature)
genre_df$Genre <- as.numeric(genre_df$Genre)
str(genre_df)
#removing outliers
genre_df$Danceability <- rm.outlier(genre_df$Danceability)
#removing outliers
genre_df$Danceability <- genre_df[rm.outlier(genre_df$Danceability)]
#removing outliers
genre_df <- genre_df[rm.outlier(genre_df$Danceability)]
#removing outliers
outliers <- boxplot(genre_df$Danceability,plot = FALSE)
outliers
#removing outliers
outliers <- boxplot(genre_df$Energy,plot = FALSE)
outliers
#removing outliers
outliers <- boxplot(genre_df$Energy,plot = FALSE)$out
outliers
#removing outliers
outliers <- boxplot(genre_df$Energy,plot = FALSE)$out
outliers
#removing outliers
outliers <- boxplot(genre_df$Danceability,plot = FALSE)$out
library(caTools)
library(randomForest)
library(outliers)
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
genre_df$time_signature <- as.numeric(genre_data$time_signature)
genre_df$Genre <- as.numeric(genre_df$Genre)
str(genre_df)
#removing outliers
outliers <- boxplot(genre_df$Danceability,plot = FALSE)$out
genre_df <- genre_df[-which(genre_df$Danceability %in% outliers)]
boxplot(genre_df$Danceability)
#removing outliers
outliers <- boxplot(genre_df$Energy,plot = FALSE)$out
genre_df <- genre_df[-which(genre_df$Energy %in% outliers)]
boxplot(genre_df$Danceability)
boxplot(genre_df)
#removing outliers
outliers <- boxplot(genre_df$Energy,plot = FALSE)$out
genre_df <- genre_df[-which(genre_df$Energy %in% outliers)]
str(genre_df)
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
genre_df$time_signature <- as.numeric(genre_data$time_signature)
genre_df$Genre <- as.numeric(genre_df$Genre)
str(genre_df)
#removing outliers
outliers <- boxplot(genre_df$Danceability,plot = FALSE)$out
genre_df <- genre_df[-which(genre_df$Danceability %in% outliers)]
boxplot(genre_df)
#removing outliers
outliers <- boxplot(genre_df$Danceability,plot = FALSE)$out
genre_df <- genre_df[-which(genre_df$Danceability %in% outliers)]
boxplot(genre_df)
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
genre_df$time_signature <- as.numeric(genre_data$time_signature)
genre_df$Genre <- as.numeric(genre_df$Genre)
str(genre_df)
boxplot(genre_df)
#removing outliers
outliers <- boxplot(genre_df$Danceability,plot = FALSE)$out
genre_df <- genre_df[-which(genre_df$Danceability %in% outliers)]
str(genre_df)
boxplot(genre_df)
#removing outliers
outliers::rm.outlier(genre_df)
#removing outliers
no_outliers <- boxplot(genre_df$Danceability,plot = FALSE)$out
genre_df <- genre_df[!genre_df %in% boxplot.stats(genre_df)$out]
genre_df <- genre_df[-genre_df %in% boxplot.stats(genre_df)$out]
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
genre_df$time_signature <- as.numeric(genre_data$time_signature)
genre_df$Genre <- as.numeric(genre_df$Genre)
genre_df <- genre_df[-genre_df %in% boxplot.stats(genre_df)$out]
outlier(genre_df)
rm.outlier(genre_df$Danceability)
outlier(genre_df)
rm.outlier(genre_df$Key)
outlier(genre_df)
rm.outlier(genre_df$Key)
outlier(genre_df)
outlier(genre_df$Key)
rm.outlier(genre_df$Key)
outlier(genre_df$Key)
rm.outlier(genre_df$Loudness)
outlier(genre_df)
library(caTools)
library(randomForest)
library(outliers)
genre_df <- genre_data[-c(1)]
genre_df <- na.omit(genre_df)
str(genre_df)
#train and Test
sample <- sample.split(genre_df,SplitRatio = 0.01)
#train and Test
sample <- sample.split(genre_df,SplitRatio = 0.001)
train <- subset(genre_df,sample == TRUE)
train <- subset(genre_df,sample == TRUE)
test <- subset(genre_df,sample==FALSE)
#modeling the the training data
train$Genre <- factor(train$Genre)
randfmodel <- randomForest(Genre ~.,data = train, ntree = 2001,importance = TRUE)
